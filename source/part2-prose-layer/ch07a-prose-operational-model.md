---
title: "第 7a 章 —— 文稿层的运维模型"
status: review
authors:
  - Walter (Yamin) Fan
last_verified_commit: HEAD
zh_status: complete
keywords:
  - prose-layer
  - operational-model
  - review
  - drift
  - gates
---

# 第 7a 章 —— 文稿层的运维模型

本章一页速览 —— 四个角合在一起，让一个知识库可以回答 *“这段文稿在当前 commit 上是不是正确的？有没有人知道？”* ；再走一个具体场景，把一次 API 变更怎么穿过这四个角**一笔画完**；最后再诚实地列一份 —— 这个模型 *并不* 声称自己能解决的那些问题：

```{mermaid}
mindmap
  root((4 corners))
    C1 Page set
      Small, stable, opinionated
      Shape readers remember
      Shape tools target
    C2 Footer
      last_updated, commit
      updated_by (human, ai, ai+human)
      review_status (pending, approved)
      review_score (0-5)
      reviewed_by
    C3 Three levels
      L1 Mechanical (script, 0 tokens)
      L2 Bounded-LLM (~1-5 k tokens)
      L3 Human-led (variable)
      Rule first, LLM second
      AI always pending
    C4 Gates
      Hard gates (block publish)
      Soft gates (warn only)
      Footer well-formed
      file:line resolves
      No TODO markers
    Not solved here
      Correctness (gates check form, not truth)
      Reader prioritisation
      Cross-page conflicts
      Semantic equivalence between prose and code
```

一个 LLM**刚刚重写了**你知识库里 **412 个页面**。它们**全部能 build**、**全部有规格良好的 frontmatter**、**全部渲染时一条坏链接都没有**。周五下午，你的 manager 发了一条消息 —— **三行字，一个问题**： *这些页面现在，我们能信了吗？*

你读了前三页，看上去没问题。你打开**第四页**。它描述的是**六周前就已经废弃的一套工作流**。**第五页**用的**函数名**，是**在同一个触发这次重生成的 PR 里被改掉的**。**第六页**完美。你**还有 406 页**，离 17:00 的 standup **大约 90 分钟**。**哪几页**必须让人**在周一早晨之前看过**？你又怎么**告诉你的 manager** *“我们还不知道”* —— 让这句话**听起来像专业**，**不像束手无策**？

**第 4–7 章**把文稿**装了进来**。这一章讲的是**蜜月期结束之后那第一个周一发生的事**：**一个内容廉价、信任昂贵的知识库**。下面这个**四角模型**，就是让你**用三行字回答你 manager 的那个问题** —— 并且**更重要的是**：**让你诚实地回答它**。

## 为什么

第 4 到第 7 章讲的都是 *把文稿收进来* ：基于文件的 wiki，frontmatter 加 footer 的契约，分类流水线，以及一个带搜索的已构建站点。这四章里的一切都是单向流水线 —— 进来一个东西，产出一个东西，章节就结束了。

真实世界的知识库不会到这里就结束。文稿底下的代码每天都在变。架构页描述的那个函数被改名了。runbook 描述的那个流程多了一个步骤。reference 页声称返回 JSON 的公开 API，现在还会发 Server-Sent Events。这些东西对任何维护过文档的人来说都不是新闻。**真正的新闻**是：既然 LLM 已经便宜到可以用几分钱改写一页文档，**约束条件已经转移了**。问题不再是 *“改文档还划不划算？”* —— 几乎一直划算。问题变成了 *“这次改动有没有被人签字确认？如果有，是哪位人，在什么时候签的？”* 瓶颈不是 embedding 成本，也不是 LLM 成本；以 `text-embedding-3-small` 在 2024 年的价格来算，把一个 10 万行代码的仓库整体重新 embed 一遍也就一美元以内 {cite}`openai_embeddings_v3` 。瓶颈是 **人类的注意力**。一个每周扔出 400 文件 pull request 的知识库，最后谁都不信 —— 不是因为它写错了，而是因为没人有空去确认它写对了。

这一章要介绍的是本书后面所有章节都要依赖的那个**运维模型**。它是作者在一个私人project-knowledge-base 工具集里一点点打磨出来的那套纪律的一个具象化{cite}`fanyamin_pkb_skill` ；这一章负责给那套结构起名字、讲清楚骨架，第 14–16 章再把每一块展开到可运维的颗粒度。这套模型**刚好有四个角**。本书后面一切关于“文稿层怎么保持正确”的论断，归根结底都能归约到其中某一个角。

## 是什么

这个模型就是四个角的集合。把它们摆在一起，要回答的是 *“这段文稿在当前这个 commit 上是不是正确的？有没有人知道？”* 每个角都有一个短标签（C1–C4），其他章节在引用它们时用标签就够了，不用把模型再重述一遍：

- **C1 —— 固定的页面集。** *一个知识库应该有哪些页面？*
- **C2 —— 元数据 footer。** *这一页，现在还值不值得信？*
- **C3 —— 三级更新策略。** *让它保持正确，代价由谁来付？*
- **C4 —— 校验闸。** *发布之前，有哪些条件必须成立？*

### C1 —— 固定的页面集

一个让目录随便长的知识库，很快就会长出一个谁都不搜的 `misc/` 文件夹。一个肯承诺“就这几页”的知识库，则给了读者一个能记住的形状、也给了工具一个可以瞄准的形状。第 3 章 *One opinionated instantiation* 那一节已经提过十页的建议清单（ `overview` 、 `quick-start` 、 `architecture` 、`repo-map` 、 `data-and-api` 、 `workflows` 、`conventions` 、 `testing` 、 `runbook` 、 `observability` ）。C1 就是那张清单 —— 被抬到运维模型里的一个角的位置。内容留在第 3 章，**规则**留在这里。**有 *某一套* 稳定的页面集，比争论“到底哪一套”要重要得多。**

### C2 —— 元数据 footer

第 5 章已经介绍过：每一张内容页都以一段 HTML-comment footer 收尾，这段 footer 有六个字段：

```markdown
<!-- PKB-metadata
last_updated: 2026-04-14
commit: 4a1c92b
updated_by: ai            # human | ai | ai+human
review_status: pending    # pending | approved
review_score: 0           # 0-5
reviewed_by:              # empty until a human reviews
-->
```

C2 的目的是让**过时这件事变得可观测**。没有 footer 时，一个知识库要回答“这页是不是过期了？”只能把正文再读一遍、再找个人问一问 —— 这件事**又贵又慢**，而它恰恰是当初建知识库时想解决的事。有了 footer 之后，一段脚本就可以把 `commit:` 和 `git rev-parse --short HEAD` 对一下，一个评审人可以一眼筛出所有 `review_status: pending` 的页，一个 LLM agent（第 21 章）甚至可以拒绝引用 `review_score` 低于某个阈值的页。完整的 schema 在第 5 章；C4 让“footer 必须存在”变成一道构建闸；C3 规定哪些值**可以**由谁来设置。

### C3 —— 三级更新策略

当底下发生变化时，到底**由谁**（或者**由什么**）来更新文档？朴素的做法往往坍塌到两种：“让人写”（太慢，基本没人真在做），或者“让 LLM 把全部重新生成一遍”（太吵，评审人淹没在改动里）。两种都不成立。作者在那个私人 skill {cite}`fanyamin_pkb_skill` 里把更新拆成了**三级**，每一级对应的成本和所需的执行者都不同：

| 级别 | 触发条件 | 执行者 | Token 成本 | 典型例子 |
|------:|---------|-------|-----------:|-----------------|
| **L1** 机械级 | 文件重命名；链接失效；commit SHA 漂了；repo-map 里多了或少了文件 | 脚本（不用 LLM） | **0** | 重新盖章 footer 里的 `commit:` ；修掉坏掉的 `]()` 链接；用 `git ls-files` 重新生成 `repo-map.md` |
| **L2** 受限 LLM | 内容漂了， *但是* 这次漂移只局限在某一页的 diff 里 | 上下文非常窄的 LLM：一页内容 + 它的 diff + 它的 footer | ~1–5 k tokens | 函数签名改了；重新生成 `data-and-api.md` 里引用到它的那**一段** |
| **L3** 人工主导 | 架构变更、新写一份 ADR、横跨多个模块的重构、涉及安全性的内容 | 人写，LLM 做文字润色 | 浮动；受人本身的上限约束 | 把一个服务拆成两个之后，重写 `architecture.md` |

有两条规则让这三级用起来是安全的：

1.  **先走规则，后用 LLM。** 每一次变更从 L1 开始。如果 L1 能搞定，一个 token 都不花。只有 L1 解决不了的才落到 L2；只有 L2 也搞不定的结构性变更才落到 L3。
2.  **AI 永远设回 pending。** 当任何自动化路径（L1 或 L2）修改了一页时，`review_status` 设为 `pending`，`review_score` 设为 `0`，`reviewed_by` 清空。只有人 —— L3 或后续的评审 —— 才能设 `approved` 或非零分数。这条规则在第 5 章声明，在第 15 章强制执行。

第 16 章会完整展开这个主题，包括一棵决策树，用来把每次改动分流到三级里的某一级，还有一张对比表：把这套策略和“一改就把整个仓库重新 embed 一遍”的朴素做法在成本上算给你看。

### C4 —— 校验闸

**一道没人强制执行的闸，就不是闸。** C4 要讲的是：运维模型嘴上说出的每一条承诺 —— *“footer 必须存在”、“footer 里的字段必须合法”、“每一个 file-line 引用在当前 commit 上都能解析出来”、“不能留下任何裸的 `[TODO]` 标记”* —— 都是一段跑在 CI 上的机械检查，要么作为**硬闸**（不通过就拦住发布），要么作为**软闸**（只报警、不拦）。完整的清单在第 15 章。

是**闸系统**让 C2 和 C3 从文字变成现实。没有闸，footer 只是**建议**；有了闸，footer 是**契约**。没有闸，“AI 永远设回 pending”只是**一个愿望**；有了闸，任何一页如果带着 `review_status: approved` 却没有对应的人类 `reviewed_by` ，构建就会失败。

### **为什么恰好是四个角？**

**读到这里的读者，有一个合理的问题**： *为什么是四？又为什么是这四个？* “**因为经验告诉我是这样**”是**诚实的答案**，但**不是一个令人满足的答案**。**结构性的答案**是：这**四个角**，**恰好是****戴明 Plan–Do–Check–Act 循环** {cite}`deming1986outofcrisis` **特化到一份文稿层制品上**的**四个相位**。**每个角**回答**一个相位**；**任意拆掉一个**，**整个循环就断掉**。

| PDCA 相位 | 它回答的问题 | 回答它的那个角 | 如果缺了这个角 |
|------------|---------------------|----------------------------|--------------------------|
| **Plan** —— 应该存在哪些东西？ | *一个知识库应该有哪些页面？* | **C1** 固定的页面集 | **自由形式的文件夹**；一个**会扩散转移**的 `misc/` 目录。 |
| **Do** —— 它怎么被更新？ | *谁 —— 或者什么 —— 在做这次更新，代价是多少？* | **C3** 三级更新策略 | **要么**每一次变更都过一遍 LLM，**要么一次都不过**；**要么代价无界**，**要么知识库变腐**。 |
| **Check** —— 这次更新可以被接受吗？ | *发布这次更新之前，哪些条件必须成立？* | **C4** 校验闸 | 规则停留在“**政策**”层面、**不被强制** —— 也就是**最终被忽略**。 |
| **Act** —— 关于这一页，我们学到了什么？ | *这一页，此时此刻，还被相信吗？被谁相信？相信到什么程度？* | **C2** 元数据 footer | **腐化是不可见的**；**评审状态活在某个工程师的脑子里**。 |

**从这张映射**出发，可以**得出三点观察**。

**这个模型是结构性地完整的，不是偶然地完整。** **任何**一套围绕“**代码在变，文档得保持正确**”构建的运维纪律，都**必须**对每一个相位**恰好给出一个答案**。**两角系统**（**只有** *Plan + Do* ：“**我们有模板，我们写更新**”）是**业界默认的形状**，也是**默认的失败形状** —— **缺了** *Check* ，**模板会悄无声息地腐烂**；**缺了** *Act* ，**知识库分不清自己和自己产生的排泄物**。**三角系统**（ *Plan + Do + Check* ：**模板 + 流程 + CI**）是**多数成熟团队能走到的位置**；但**它们依然脆弱** —— **因为** *Act* ，**那份按页记忆的功能**，**没有被机械化**。**加上 C2** —— **一段人能读、脚本能过滤的 footer** —— **就是最后那一个角**，**也是最便宜的那一个**。

**四个角是相互独立的，也是全都承重的。** 它们**相互独立** —— **因为每一个都回答 PDCA 的一个不同相位**，而**任何一个角的机制****都没法替代另一个角**：**一份信息丰富的 footer** （C2）**不会告诉你****应该存在什么东西**（C1）；**一道严格的校验闸**（C4）**不会告诉你****谁来付这次更新的代价**（C3）。它们**都承重** —— **像一张凳子的四条腿** —— **抽掉任意一条**，**整个结构就塌**。**有固定页面集、有 footer、有闸**，**但没有更新策略**的知识库（**缺 C3**），**在第一次代码变更之前都能工作**，**然后立刻被无人预算去修的 `pending` 页面淹没**。**三套机制都有、但没有固定形状**的知识库（**缺 C1**），**没法告诉读者 *该到哪里看*** ，于是**C2、C3、C4 都没有被足够频繁地行使**，**也就无法保持诚实**。

**这组对应也解释了第四部分接下来讲述它们的 *次序* 。** **第 14 章**走**变更检测**，这本身是**一次由** *Act* 观测（C2 footer 的 SHA diff）**所触发的** *Do* 操作（C3）。**第 15 章**把**闸变成真**（C4）。**第 16 章**把**三级更新**连起来（**真正意义上的 C3**）。这本书搭建的循环是 **Do → Check → Act → 再回到 Plan** —— **因为这才是****这个循环在运维时间上真正运行的次序**； *Plan* 角（C1）**是边界条件，不是周期性的一步**。

**这张映射并不是一次关于最小性的形式证明** —— **PDCA 本身**也**只是一套启发式、不是一个定理**。但它**给了四个角一个读者可以推理的形状**，并且**更重要的是**：**一个读者可以 *拒绝* 的形状**：**如果一个团队****发现自己的知识库真的需要第五个角**，**有效的做法不是把它硬塞进 C1–C4 里的某一个**，**而是扩展这个循环**。**这个模型是地板，不是天花板。**

## 怎么做

这四个角怎么串起来？我们走一个具体的场景就清楚了。假设代码库里一个公开 API 路由改了 —— `/api/v1/pages` 这个 endpoint 多了一个查询参数。接下来按顺序会发生什么：

1.  **变更检测（第 14 章，通过 C2）。** 一个夜间任务把 `git HEAD` 和每个 footer 中 `commit:` 值的集合做 diff。它发现 `data-and-api.md` 和 `api-reference.md` 里列的都是旧的 commit，并且 diff 触及了 `api-reference.md` 以 `file:line` 引用的代码。两个候选页面。
2.  **L1 分流（C3）。** 任务对两个候选跑 L1 检查。对 `data-and-api.md`，唯一的漂移是 footer SHA —— 正文仍然正确描述了那条路由。L1 重新盖章 footer：`commit:` 刷新，`last_updated:` 刷新，`updated_by: ai`，`review_status: pending`（AI 永远设回 pending 规则），`review_score: 0`。零 token 消耗。对 `api-reference.md`，漂移是实质性的 —— 有一段引用了旧的参数列表。L1 修不了这个；它升级。
3.  **L2 分流（C3）。** 用极窄的上下文调用 LLM：当前 `api-reference.md` 正文（~2 kB）、相关的代码 diff（~300 B）和页面的 footer。它返回受影响的那一段的新版本。footer 按同一条规则重新盖章：`review_status: pending`，`reviewed_by:` 清空。成本：约 2.3 k token —— 不到一美分。
4.  **闸运行（C4）。** 发布之前，硬闸触发。footer 在两个页面上都存在且格式良好：通过。`api-reference.md` 里的每一个 `file:line` 引用在当前 commit 上都能解析（包括新参数 —— L2 刚刚引用了它）：通过。没有引入 `[TODO]` 标记：通过。Mermaid 图没变动且仍然可以解析：通过。发布继续。
5.  **人工评审（C3 L3 → C2）。** 评审人在知识库的评审仪表盘上看到两页 `review_status: pending`，读了两份 diff（不是 400 个文件 —— 只有两个），批准两者，并分别设 `review_score: 4`。下一次 L1 检查这两页时，会让 `review_status` 保持在 `approved`，因为正文没有发生变更。

以上这段序列里，没有任何一步是实验。每一步不是一段脚本就是一条规则，而它们存在的唯一原因是四个角里某一个角把它安排在那里。变更检测能跑起来，是因为 C2 让过时这件事变得可观测。分流能跑起来，是因为 C3 给三级都赋予了稳定的含义。发布能发出去，是因为 C4 拒绝放行任何一张格式不对的页。而下一个打开这份文档的人，只要看任何一张页的 footer ，就能在不问任何人的情况下知道：这一页被 LLM 改过，而且有一个具名的人已经签过字了。

## 示例

把这个循环收缩到最小可用形态：跟踪一张页的 footer 在一周里的变化，讲的就是下面这段：

```text
Monday  08:00  commit: a1b2c3d4  updated_by: human  review_status: approved   reviewed_by: alice
                 (page written by Alice; nothing else changes all week)

Monday  08:15  commit: a1b2c3d4  updated_by: human  review_status: approved   reviewed_by: alice
                 (L1 runs against HEAD; commit still matches; no change)

Wednesday 14:30  commit: 4a1c92b  updated_by: ai    review_status: pending    reviewed_by:
                 (API changed; L2 regenerated one paragraph; footer reset)

Thursday 10:05  commit: 4a1c92b  updated_by: ai+human  review_status: approved  review_score: 4  reviewed_by: alice
                 (Alice read the L2 diff and approved)

Friday  23:00  commit: 9f8e7d6c  updated_by: human  review_status: pending   reviewed_by:
                 (an unrelated refactor moved a linked file; L1 fixed the broken link;
                  review resets because the body was touched)
```

五个状态、一张页、一段完整的审计轨迹，全都躺在它自己的 footer 里。没有任何附表，没有任何 CI 数据库，没有任何评审电子表。一个人用 `cat` 或 `vim` 打开文件，就能把整段历史看完。一个脚本在查询这个知识库时，拿到的是一套**封闭词汇表**：`approved | pending` ， `human | ai | ai+human` ， `0–5` —— 可以零歧义地筛选。

L1/L2/L3 完整的分流器、决定哪些页面算候选的变更检测层、以及闸规则的完整清单，是接下来三章的主题。

## 这个模型 *并没有* 解决的问题

四个角做的事，是让一个知识库**关于自己正确性的那些说法**变得可审计。它们**并不**因此就让那些说法 *变成真话* 。这个区分值得专门说一遍，因为很多团队把运维模型当成**全部的活**，而不是**支撑那些活的脚手架**，结果漂到一种具体而又可避免的失败形态：每一页的 footer 都写着 `approved` ，而内容仍然是错的。

有四条边界值得点名说清楚。

1.  **Gates check *form*, not *truth*.** The verification gates in
    C4 能机械地确认 footer 存在、`file:line` 引用在当前 commit 上能解析、以及没有留下 `[TODO]` 标记。但它们无法确认紧挨在 `file:line` 锚点旁边的那段文字*是不是*对它所指向的那个函数的正确描述。那种检查需要的要么是一个读了那页的人，要么是一个交叉阅读了代码的 LLM —— 而这恰恰是三级更新策略（C3）去分派的东西，但分派器本身对语义真理的验证能力，并不比编译器对"一个函数是否真的做了它名字所声称的事"的验证能力更强。

2.  **`approved` means "a human read this diff", not "this is
    正确的"。** "AI 永远设回 pending"规则确保了一份 `approved` 的页面是由一个具名的人签了字的，闸目录确保了这个人看的是当前 commit。但这两个机制都无法阻止一个评审人批准了一份有微妙错误的 L2 重写 —— 因为评审人赶时间，或者因为错误藏在评审人不熟悉的领域角落。这个模型让评审变得*可追溯*，而不是*不可犯错*。第 15 章为安全敏感页面增加了双评审人规则；低风险页面保持单评审人路径，审计轨迹作为补偿控制。

3.  **这个模型不会替读者排优先级。** 一个有一万页、footer 全是 `approved` 的知识库，仍然迫使读者自己决定先读哪一页。排序、策展、废弃机制和 Diátaxis 结构（第 3 章）才是让知识库*好用*的东西；运维模型让它*可信*。这是两个独立的性质，单独一个都不够。一本书的索引不是目录。

4.  **跨页冲突不在范围内。** 如果 `architecture.md` 和 `runbook.md` 描述的是同一个系统却互相矛盾，两者可以同时以 `review_status: approved` 和 `verification_status: supported` 的状态待在知识库里。检测这种矛盾需要把页面互相比对，而四角模型不做这件事。第 18 章的文档分层四元组和第 21 章的 agent 可访问知识库合在一起开始解决这个问题 —— 一个读了两页的 agent 可以*注意到*冲突 —— 但仅凭文稿层运维模型做不到。一个有矛盾页面的知识库，是一个运维模型在正常工作的知识库；需要去捕获矛盾的，是*编辑*模型。

把这四个角看得恰当的方式是：它们是一个知识库**为了不再对自己说谎**所需要的那套纪律。至于它**关于它所描述的那个软件**是不是也在说真话，那是另一个更大的问题 —— 因为那个问题没法机械回答，所以“让人类的注意力变得可追责”的那套脚手架，才是一个随着时间推移、**替你一点点把那个问题答出来**的东西。

## 一页总览第二部分 —— 一张跳转清单

**横跨这五章**，**第二部分**一共编目了**大约二十几种****失败模式、误解、和接线错误**。**对于那些 *已经部署过一个知识库之后* 回头翻这本书**、**正在试图诊断一个具体症状的读者**，下面这张表是**一份单页索引** —— **按团队实际看到的症状组织**，**而不是按它住在哪一章组织**。

### **存储与仓库卫生**（第 4 章）

| **你看到的症状** | **根本原因** | **一句话的补救** | **去哪里看** |
|-----------------|------------|-----------------|---------|
| **克隆一个月比一个月慢** | `content/` 里被提交了二进制大块 | CI 对文件大小设硬上限；**少数合理的二进制**走 Git LFS | 第 4 章 §“Common mistakes” #1 |
| **代码里指向 wiki 页面的外链**在一次改名之后全挂 | **改了 slug 却没有重定向表** | 把 `redirects.yaml` check in；**CI 拒绝任何会打破仍被引用 URL 的 PR** | 第 4 章 §“Common mistakes” #2 |
| **PR UI 根本没法有效展示的合并冲突** | 两个作者**并行重写**了同一个文件 | 把**高频变动**的页面当作 *有 owner* 的；**现场事故文档**用共享文档，**事后再导入** | 第 4 章 §“Common mistakes” #3 |
| **按读者的历史/自动保存/评论**把仓库撑大 | **把“万物皆文件”过度延伸到可变的运维状态上** | **可变状态住在旁边的数据库里**；**文件只放内容** | 第 4 章 §“Common mistakes” #4 |
| **尽管有 PR 纪律，文档依然和代码漂移** | **文档评审走在一条和代码评审不同的沟通线上** | docs-as-code **通过康威定律 / Parnas 共处一地把两者重新对齐** | ch04 §"Why docs-as-code really works" |

### **来源与信任**（第 5 章）

| **你看到的症状** | **根本原因** | **一句话的补救** | **去哪里看** |
|-----------------|------------|-----------------|---------|
| **一份老的审计记录悄无声息地丢了作者** | 在一次批量迁移里，`created_by` 被当作**可变的** | `created`、`created_by`、`source` **冻结**；**只能通过显式的 PR 更正** | 第 5 章 §“Common mistakes” #1 |
| `content/` 的 `git log` 被**评审状态切换**淹没 | **评审状态**住在 frontmatter 里，**而不是自己的独立块里** | **拆成**：frontmatter（**谱系**）+ footer（**评审状态**） | 第 5 章 §“Common mistakes” #2 |
| **生产的知识库里现在有 `approved` 页面**，**没有人类看过** | **`approved` + `reviewed_by` 上没有硬闸** | **CI 闸拒绝**：`approved` **而没有对应的人类 `reviewed_by`** | 第 5 章 §“Common mistakes” #3 |
| `doc_type` 的**词汇表**已经漂成**一个乱糟糟的长列表** | **校验器里没有把枚举闭合** | `doc_type` 是一个 *封闭* 的枚举；**在校验时拒绝未知值** | 第 5 章 §“Common mistakes” #4 |
| **被标记 `supported` 的页面**，**对应的 commit 已经被 rebase 改写过两次** | `verification_status` **没有相对** `commit` **被重新评估** | **腐化探测器**在 footer 的 `commit`**不再匹配它所引用的代码时**，**把信任降级** | 第 5 章 §“Common mistakes” #5 |
| `source.uri` 指向**一个 404**，**或者一个悄悄被改了措辞的 URL** | `source` **没有 `ref`** | **每一个** `source.uri` **都必须带一个** `ref` ：commit SHA、Wayback 快照、或内容 hash | 第 5 章 §“Common mistakes” #6 |

### **分类流水线**（第 6 章）

| **你看到的症状** | **根本原因** | **一句话的补救** | **去哪里看** |
|-----------------|------------|-----------------|---------|
| **导入进来的页面**被归到 `security-architecture` ，**且信任是** `supported` | **通过导入内容注入 prompt** | **给分类器的输入上界限**；**显式标注不可信内容**； `verification_status` **不在 LLM 的输出 schema 里** | 第 6 章 §“LLM 的失败模式” #1 |
| **`content/` 里出现了一个新编出来的类别** | **幻觉出来的标签**（**封闭枚举没有被强制**） | **调用方拒绝枚举之外的标签**；**回退到启发式分类器** | 第 6 章 §“LLM 的失败模式” #2 |
| **几个月下来，类别分布在缓慢漂移** | **模型的静默升级**，**或者语料的漂移** | **往仓库里 check 一个 ~50 页的 gold set**；**夜间 CI diff 标签** | 第 6 章 §“LLM 的失败模式” #3 |
| **周末一半的 ingest 悄无声息地走了启发式** | **JSON 模式解析失败被记了日志，但没有被做成度量** | **用结构化输出 API**；**把解析失败当成一等公民的度量** | 第 6 章 §“LLM 的失败模式” #4 |
| **ingest 批次的成本一夜之间爆炸** | **供应商定价 / 限流变了** | **每批次硬预算**：最大 token、最大墙钟时间、**中止并 commit 部分结果** | 第 6 章 §“LLM 的失败模式” #5 |
| **一年之后，导入页面里的图片 404** | **导入器没有重写资源 URL** | **由 *ingest 路径* （同一个函数）负责 URL 重写**；**每个导入器都流经它** | 第 6 章 §“LLM 的失败模式” #6 |
| **团队把 Diátaxis 标签当作 ground truth** | 分类 *就是* 一次有损投影 | **把分类器的不确定性记录下来**；**低置信度路由到人审**；**把 `unreviewed` 当作诚实的默认值** | ch06 §"A note on what classification *is*" |

### **搜索与发布**（第 7 章）

| **你看到的症状** | **根本原因** | **一句话的补救** | **去哪里看** |
|-----------------|------------|-----------------|---------|
| **用户搜** `throttle incoming traffic` ，**零结果** | **和 identifier-literal 之间的词汇失配** | **先把 frontmatter 的** `tags:` **变丰富**；**等这招也不行时再上 dense retrieval** | 第 7 章 §“这个小引擎会在哪里失败” #1 |
| 在中文语料上搜 `配置文件` **返回零结果** | **没有分词**；`unicode.IsLetter` **不会切分** | 把 `tokenize` **换成**一个中文分词器（`go-jieba` 、`kagome`） | 第 7 章 §“这个小引擎会在哪里失败” #2 |
| **`recieve` 返回零结果，用户抓狂** | **没有拼写容错** | **词数在 ~10 万以内**：**进程内做 fuzzy matching**；**超过之后换 Meilisearch** | 第 7 章 §“这个小引擎会在哪里失败” #3 |
| **冗长但不相关的页面**排在**聚焦的页面**前面 | **打分用的是词命中数，没有长度归一化** | **在 `title` 上做字段加权**；**这个模式持续出现时**，**升级到 BM25** | 第 7 章 §“这个小引擎会在哪里失败” #4 |
| **增量索引陈旧，但 debug 又很难** | **增量更新**就是**大多数数据库型 wiki 的失败模式** | **写的时候整张表重建**；在这个规模上只是毫秒级 | 第 7 章 §“接线搜索时的常见错误” #1 |
| **在线服务和静态站点返回不同的结果** | **两层搜索在并行运行** | **阶梯是一条 *序列* —— 只选一层，只保留一个接口** | 第 7 章 §“接线搜索时的常见错误” #2 |
| **`contradicted` 的页面在默认搜索里冒出来** | **信任字段还没被当成检索用的元数据** | **查询时按 `verification_status` 过滤**；**给一个显式的“包括已过时页面”开关** | 第 7 章 §“接线搜索时的常见错误” #3 |
| **组织把 Sphinx 的客户端搜索给已登录用户用** | **静态搜索看不见 frontmatter / 信任字段** | **服务端搜索是权威的**；**静态搜索给的是公开 / 离线场景** | 第 7 章 §“接线搜索时的常见错误” #4 |

### **运维模型**（第 7a 章）

| **你看到的症状** | **根本原因** | **一句话的补救** | **去哪里看** |
|-----------------|------------|-----------------|---------|
| **每一页都 `approved`，内容依然是错的** | **闸检查的是形式，不是真相** | `approved` **表示** *一个人读过这次 diff* ，**不是** *这是对的* ；**对安全敏感页面加第二评审人规则** | 第 7a 章 §“这个模型 *并没有* 解决的问题” #1、#2 |
| **一万个 `approved` 页面，用户还是找不到对的那一个** | **模型让知识库变得可信，不是变得** *可用* | **优先排序是 Diátaxis + 策展**，**和信任是正交的** | 第 7a 章 §“这个模型 *并没有* 解决的问题” #3 |
| **`architecture.md` 和 `runbook.md` 互相矛盾，都是 `approved`** | **四角模型是按页的，不是跨页的** | **跨页冲突检测**在**第二部分的范围之外**；**第 18 章 + 第 21 章开始处理它** | 第 7a 章 §“这个模型 *并没有* 解决的问题” #4 |

**贯穿这整张表的一条共同线索**： *这些失败里的每一个，都有一个廉价的机械修法 **以及** 一个昂贵的“重新架构”的修法；而团队一贯先去尝试那个昂贵的。* **所谓运维模型**，**就是****在花一周时间做那个昂贵修法之前**、**先花一个小时试那个廉价修法的习惯**。

## 结论

第二部分的四个角（页面集、元数据 footer 、三级更新策略、校验闸）是第三部分那四个角（解析、embedding、图谱、prompt）在文稿层的对应物。每一层都有一个**本书会反复回来的稳定 *结构*** ，以及一个**让这个结构变成现实的稳定 *机制集*** 。第四部分剩下的内容，是**怎么把文稿层的这些机制接起来**；第五部分讲的是**两层之间怎么互相对话**。

这四个角本身并不自称能**产出正确的文档**。它们只承诺：一个实现了这四个角的知识库，可以 *诚实地回答* 关于它自己正确性的问题 —— 在必要时，包括回答一句 *“我们不知道；这一页还没被评审过。”* 第 18 章的文档分层元组、以及第 21 章那个可被 agent 调用的知识库接口，都是建在这句诚实的回答之上的。

## 参考文献

```{bibliography}
:filter: keywords % "prose-layer" or keywords % "operational-model" or keywords % "self-citation" or keywords % "foundations"
```
