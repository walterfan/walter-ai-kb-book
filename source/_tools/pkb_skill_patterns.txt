# PKB-skill non-copy patterns.
#
# This file is consumed by `source/_tools/check_pkb_quotes.py`. See
# `openspec/changes/pkb-skill-absorption/design.md` §5 for the rationale.
#
# Format:
#   # <comment>            — ignored
#   forbid:<string>        — <string> MUST NOT appear anywhere in source/**/*.md
#   copy:<string>          — <string> (≥40 chars) MUST NOT appear verbatim in
#                            source/**/*.md outside a paragraph that also
#                            contains {cite}`fanyamin_pkb_skill`.
#
# Matching is literal and case-insensitive. Leading/trailing whitespace on
# the pattern value is stripped. An empty value is ignored.

# ── Redaction invariants (forbid entirely) ──────────────────────────────────

# Internal URLs
forbid:docs.zoom.us
forbid:dg01docs.zoom.us
forbid:git.zoom.us
forbid:git.ops.corp.zoom.us
forbid:qa.zoomdev.us
forbid:jenkins.zoom.us
forbid:jenkins.client.corp.zoom.us
forbid:artifacts.corp.zoom.us
forbid:eng.corp.zoom.com
forbid:new-dayone.zoomdev.us
forbid:zoomvideo.atlassian.net
forbid:ai-hub.corp.zoom.us

# Internal product / service names
forbid:AgentBox
forbid:Agent Box
forbid:DayOne
forbid:Day One
forbid:TestZoom
forbid:Async MQ
forbid:Async Pilot
forbid:AI Hub
forbid:DevHelper
forbid:Productboard

# Internal CLI / tool names
forbid:zcp-cli
forbid:mirrord-zcp
forbid:csms_tool
forbid:agentbox.yaml
forbid:csms.properties
forbid:PKB_ROOT

# /PKB-* command syntax
forbid:/PKB-init
forbid:/PKB-overview
forbid:/PKB-architecture
forbid:/PKB-adr
forbid:/PKB-testing
forbid:/PKB-verify
forbid:/PKB-review

# Employer email domains
forbid:@zoom.us
forbid:@corp.zoom.us
forbid:@zoom.com

# Private repo paths
forbid:zoom-dev-skills
forbid:workspace/zoom/

# ── Prose non-copy patterns (copy:) ─────────────────────────────────────────
#
# Populated conservatively as we observe paraphrase-to-reproduction risk
# during Phases 1–6. MIN length is 40 characters; the checker enforces that.
#
# Intentionally empty at Phase 0.
